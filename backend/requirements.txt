# Runtime dependencies for the Ledger engine.
#
# Deliberately minimal so the service builds and boots in a 512MB container.
# Anything imported lazily inside a try/except lives in requirements-optional.txt
# instead — see the note there before adding something here.

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9
python-dotenv==1.0.1

# ── Data + statistics (the deterministic core — A1, A5) ───────────────────────
pandas==2.2.1
numpy==1.26.4
scipy==1.12.0
statsmodels==0.14.1
openpyxl==3.1.2

# ── Visualisation (A10) ───────────────────────────────────────────────────────
plotly==5.20.0

# ── Exports ───────────────────────────────────────────────────────────────────
# Pure Python, no cairo/pango/headless browser, so the PDF export fits the same
# small container as everything else. Imported lazily in main.py all the same.
fpdf2==2.8.4

# ── Model providers ───────────────────────────────────────────────────────────
# Ollama is reached over plain HTTP, so it needs nothing beyond requests.
requests==2.32.3
groq==0.4.2

# ── Telemetry ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.28
